<% if @entry.errors.any? %>
  $('.my-entries-cards').html("<%= j render 'entries/form' %>")
<% else %>
  $('.cards-container').append(
      "<%= j render 'entries/card', entry: @entry %>"

    )
<% end %>
